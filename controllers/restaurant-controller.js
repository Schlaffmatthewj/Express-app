const Restaurant = require('../models/Restaurant');

const restaurantController = {};

restaurantController.index = (req, res, next) => {
    // surely you will get used eventually
};

module.exports = restaurantController;